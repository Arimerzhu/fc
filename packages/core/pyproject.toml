[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fc-core"
version = "0.1.0"
description = "FreeCAD CLI Core — Backend abstraction and API"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/fc_core"]
